{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "run the makefile",
            "command": "make",
            "options": {
                "cwd": "${workspaceFolder}/src/compiler/fe"
            },
            "args": [
                "debug=1 all"
            ],
            "problemMatcher": [],
        }
    ]
}